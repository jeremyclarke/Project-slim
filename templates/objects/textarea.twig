{% if object.required == '1' %}
    {% set required = 'required' %}
{% else %}
    {% set required = '' %}
{% endif %}

<textarea class="form-control" rows="4" cols="50" name="textarea_{{ object.ID }}" id="textarea_{{ object.ID }}" {{ required }}>
</textarea>
