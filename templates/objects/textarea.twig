{% if objects.required == '1' %}
    {% set required = 'required' %}
{% else %}
    {% set required = '' %}
{% endif %}

<textarea class="form-control" rows="4" cols="50" name="textarea_{{ objects.ID }}" id="textarea_{{ objects.ID }}" {{ required }}>
</textarea>
