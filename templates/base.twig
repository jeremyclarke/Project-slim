<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/html">
<head>
    {% block head %}
        <meta charset="utf-8">
        <meta http-equiv="X-UA-COMPATIBLE" content="IE=edge">
        <meta name="viewport" content="width = device-width, initial scale = 1">

        <!-- Bootstrap -->
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="/css/footer.css">
        <link rel="stylesheet" type="text/css" href="/css/sweetalert.css">

        <style>
            .modal-header, h4, .close {
                background-color: #5cb85c;
                color: white !important;
                text-align: center;
                font-size: 30px;
            }

            .modal-footer {
                background-color: #f9f9f9;
            }
        </style>


        <title>
            {% block title %}{% endblock %}
        </title>
    {% endblock %}
</head>

<body>
<!-- Static navbar -->
<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            {#<a class="navbar-brand" href="{{ rootURL }}">Home</a>#}
            <a class="navbar-brand" href="{{ path_for('home') }}">Home</a>

        </div>
        <div id="navbar" class="navbar-collapse collapse">
            {#<ul class="nav navbar-nav">#}
                {#<li class="dropdown">#}
                    {#<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"#}
                       {#aria-expanded="false">Forms <span class="caret"></span></a>#}
                    {#<ul class="dropdown-menu">#}
                        {#<li role="separator" class="divider"></li>#}
                        {#<li class="dropdown-header">Public Forms</li>#}
                        {#{% for form in formsAllPublic %}#}
                            {#<li>#}
                                {#<a href="{{ path_for('form', { 'id' : form.ID } ) }}">{{ form['title'] }}</a>#}
                            {#</li>#}
                        {#{% endfor %}#}

                        {#<li role="separator" class="divider"></li>#}

                        {#{% if user is defined %}#}
                            {#<li class="dropdown-header">Private Forms</li>#}

                            {#{% for form in formsAllPrivate %}#}
                                {#<li>#}
                                    {#<a href="{{ path_for('form', { 'id' : form.ID } ) }}">{{ form['title'] }}</a>#}
                                {#</li>#}
                            {#{% endfor %}#}

                        {#{% endif %}#}

                    {#</ul>#}
                {#</li>#}
            {#</ul>#}
            <ul class="nav navbar-nav navbar-right">
                {% if user is not defined %}
                <li><a href="#" data-toggle="modal" data-target="#myModal">Login</a></li>
                {% else %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                       aria-expanded="false">{{ user.first_name }} {{ user.last_name }} <span
                                class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path_for('logout') }}">Log out</a></li>
                        {% if user.admin %}
                            <li><a href="{{ path_for('admin') }}">Admin Panel</a></li>
                        {% endif %}

                    </ul>
            </ul>
            {% endif %}
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>

<div class="container">
    {% block content %}{% endblock %}
</div>

<div class="footer">
    {% block footer %}
        <div class="container">
            <p class="text-muted pull-right">
                &#169; {{ "now"|date("Y") }} Jeremy Clarke
            </p>
        </div>
    {% endblock %}
</div>

{% include 'login.twig' %}

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
<script src="/js/sweetalert.min.js"></script>

<script>
    // Attach a submit handler to the form
    $("#loginForm").submit(function (event) {

        // Stop form from submitting normally
        event.preventDefault();

        // Send the data using post
        var posting = $.post("{{ path_for('login') }}", $(this).serialize());

        // Show the results
        posting.done(function (data) {
            console.log(data);
            if (data.msg) {
                swal({
                        title: "Oops!",
                        text: data.msg,
                        type: "error"
                    }
                );
            } else {
                swal({
                        title: "Logged in",
                        type: "success",
                        timer: 0,
                        showConfirmButton: false
                    },
                    function () {
                        location.reload();
                    });
            }
        });
    });
</script>

{% block js %}
{% endblock %}


</body>
</html>